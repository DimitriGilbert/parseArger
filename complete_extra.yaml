parseArger *--output:
- <file>

parseArger bulk-parse*--file:
- <file>

parseArger bulk-parse*--directory:
- <directory>

parseArger document*--file:
- <file>

parseArger document*--out:
- <file>

parseArger document*--directory:
- <directory>

parseArger completely*--subcommand-directory:
- <directory>

parseArger completely*--subcmd-dir:
- <directory>